# GitHub token — optional, gives rate-limit headroom. The repo itself is
# hardcoded in src/lib/github.ts (GITHUB_REPOSITORY from CI env, else default).
GITHUB_TOKEN=ghp_your_personal_access_token

# Redis/Valkey response cache (both speak RESP) — empty → direct octokit hits.
# Set REDIS_PASSWORD when the server requires auth (or embed creds in REDIS_HOST).
REDIS_HOST=
REDIS_PASSWORD=

# OpenTelemetry collector: traces + logs (e.g. "localhost:4318") — empty → off.
OTEL_HOST=

# Umami analytics — script omitted when empty.
UMAMI_SRC=
UMAMI_ID=